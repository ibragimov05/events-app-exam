import 'package:flutter/material.dart';

class CanceledEvents extends StatelessWidget {
  const CanceledEvents({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
